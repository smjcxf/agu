# 交接文档 — 九宝量化 V6.0 260701满意版

**日期**：2026-07-01  
**交接人**：阿狸咪 → 小九  
**版本**：九宝量化 V6.0 260701满意版

---

## 📦 满意版归档位置

> `E:\workspace\stock-scanner\handover_小九\九宝量化V6.0 260701满意版\`

**状态**：定版保留（不参与自动覆盖，新版本另建目录，勿覆盖此版）

---

## 🔑 本次调整内容（2026-07-01）

### 1. 总览页卡片顺序调整
- 文件：`E:\workspace\merged-dist\index.html`
- 调整：🚀 打新研判 放到 ⚠️ 异动停牌观测 上方

### 2. 逻辑详解 Tab 确认
- 当前 `dist/index.html` 含 🔒 逻辑详解 tab（`admin-only`）
- 此版本为满意版归档

---

## 📋 核心信息速查

| 项目 | 内容 |
|------|------|
| 部署地址 | `https://ah-quant999.github.io/quant-scanner-v6/` |
| 本地路径 | `E:\workspace\stock-scanner\` |
| 部署命令 | `python deploy_now.py --force` |
| 数据更新 | `python batch_update.py <mode>` |
| Tab列表 | 总览/市场异动/AI速览/资金流向/预判信号/宏观观测/历史追踪/逻辑详解 |
| 备份策略 | 每日21:00 `enhanced_backup.py` → `backup_YYYYMMDD/` |
| 自动部署 | 每个定时任务末尾自动执行 deploy_now.py --force |

---

## 🔄 回滚步骤

如果后续改动出现问题，按以下步骤回滚到此满意版：

```bash
# 1. 恢复前端文件
cp -r "E:\workspace\stock-scanner\handover_小九\九宝量化V6.0 260701满意版\dist\*" "E:\workspace\stock-scanner\dist\"

# 2. 重新部署
cd E:\workspace\stock-scanner
python deploy_now.py --force
```

---

## ⚠️ 注意事项

1. 满意版为 **定版保留**，新版本归档请另建目录，不要覆盖到此目录
2. 更新 `enhanced_backup.py` 的清理逻辑，确保 `handover_小九/` 目录不在自动清理范围内
2. 合并后的总览页卡片顺序修改在 `merged-dist/index.html`，不在 dist/ 中
3. 逻辑详解 tab 是 `admin-only`，非管理员看不到
4. 定时任务配置（10个 automation）按现有 rrule 运行，无需改动
