# 交接文档 — 2026-06-26 给 小九

> 阿狸咪今晚的改动汇总，git pull 即可同步。

---

## 今晚改动（6项，全部已部署）

### 1. 个股查询金股池双区块合并
- **问题**：个股查询结果中"金股池收录"出现两个独立卡片，信息重复
- **修复**：合并为一张完整大卡，四格信号条+EMA趋势并入主卡
- **文件**：`index_master.html`

### 2. EMA描述通俗化
- **原文**：`7组多周期EMA(3/6/9→21/34/68)+FORCAST方向`
- **改为**：`短中长期均线方向，判断当前处于上升/下跌趋势`
- **文件**：`index_master.html`

### 3. 历史追踪三阶段窗口改造
- **原设计**：只有21天追踪 + 60天过期
- **新设计**：21天(短线爆发) → 60天(中期验证) → 90天(过期)
- **文件**：`triple_resonance.html`、`index_master.html`
- **注意**：第一批6月初入榜的票约7月初满60日，届时才会出现中期验证数据

### 4. 健康看板排序优化
- 停更数据(maxDays=999)排最后，低频数据(>7天)排中间，正常排前面
- **文件**：`index_master.html`

### 5. 主力个股动向彻底隐藏
- 卡片内不再显示任何过期提示，静默隐藏
- **文件**：`index_master.html`

### 6. 板块资金接口修复
- **原问题**：akshare的`stock_board_*_flow_em` API不可用，长期用mock随机数据
- **修复**：新增neodata备选数据源，降级链：akshare → neodata(真实) → mock(兜底)
- **注意**：neodata目前返回约6条（3行业+3概念），比之前15条mock少但真实
- **token**：从WorkBuddy缓存读取（12h有效期），过期自动退mock
- **文件**：`fetch_sector_fund_flow.py`

---

## 操作备忘

```bash
# 小九拉最新代码
git pull origin main

# 如有需要重新构建
python update_data_v2.py --fast
python deploy_now.py --force
```

---

## 已知问题（无需处理）

- 北向资金：数据源已停更(2024.5)，健康看板已标注
- 主力个股：数据源已停更，已隐藏
- deploy_now.py git pull报错：不影响部署（老bug）
- concept_ranking.json / watch_result.json：东财API拒绝，待观察

---

阿狸咪 2026-06-26 00:51
